<.admin_shell
  flash={@flash}
  title="Add Provider"
  subtitle="Create a new provider on a dedicated page, then return to the provider table."
  status={@status}
  admin_user={@admin_user}
  nav={@nav}
>
  <section class="sw-card">
    <div class="sw-inline-actions">
      <a href={~p"/settings/llm"} class="sw-btn sw-btn-ghost">Back to provider table</a>
    </div>
  </section>

  <section class="sw-card">
    <h2 class="sw-card-title">New Provider</h2>
    <form action={~p"/settings/llm/providers"} method="post" class="sw-form-grid mt-5">
      <input type="hidden" name="_csrf_token" value={Plug.CSRFProtection.get_csrf_token()} />

      <label class="sw-field">
        <span>Display Name</span>
        <input name="llm[name]" class="sw-input" placeholder="Cerebras Primary" />
      </label>

      <label class="sw-field">
        <span>Slug (optional)</span>
        <input name="llm[slug]" class="sw-input" placeholder="cerebras-primary" />
      </label>

      <label class="sw-field">
        <span>Provider</span>
        <select name="llm[provider]" class="sw-select">
          <%= for {label, value} <- @provider_options do %>
            <option value={value}>{label}</option>
          <% end %>
        </select>
      </label>

      <label class="sw-field">
        <span>Model</span>
        <input
          name="llm[model]"
          class="sw-input"
          value="local-default"
          placeholder="gpt-oss-120b / claude-3-5-haiku-latest / gpt-4o-mini / llama3.1"
        />
      </label>

      <label class="sw-field">
        <span>Timeout (seconds)</span>
        <input
          name="llm[timeout_seconds]"
          type="number"
          min="1"
          max="3600"
          class="sw-input"
          value="600"
        />
      </label>

      <label class="sw-field sw-field-span">
        <span>Base URL (optional)</span>
        <input
          name="llm[base_url]"
          class="sw-input"
          placeholder="https://api.cerebras.ai/v1 or https://api.anthropic.com"
        />
      </label>

      <label class="sw-field sw-field-span">
        <span>API Token (optional)</span>
        <input name="llm[api_token]" type="password" class="sw-input" />
      </label>

      <label class="sw-checkbox-row">
        <input type="checkbox" name="llm[enabled]" value="1" checked />
        <span>Enabled</span>
      </label>

      <label class="sw-checkbox-row">
        <input type="checkbox" name="llm[is_default]" value="1" />
        <span>Set as default</span>
      </label>

      <div class="sw-field-span sw-actions">
        <button type="submit" class="sw-btn sw-btn-primary">Add Provider</button>
      </div>
    </form>
  </section>
</.admin_shell>
